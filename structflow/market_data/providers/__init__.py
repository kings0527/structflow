"""Provider modules for the structured market data channel.

Third-party libraries are imported lazily inside each fetch function so
the core package never fails to import when extras are not installed.
"""
